<UserControl x:Class="Terminal.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:Terminal.Views"
             mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Label Content="Server" />
            <TextBox x:Name="serverIp" Text="{Binding ServerIp}" Width="125"/>
            <Label Content=":"/>
            <Controls:NumericUpDown Minimum="10000" Maximum="65535" Width="50" HideUpDownButtons="true" Value="{Binding ServerPort}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal"  Margin="5">
            <Button HorizontalAlignment="Left" Margin="2" Style="{StaticResource AccentedSquareButtonStyle}" Width="125" Content="Login Using FiveM" Command="{Binding LoginViaFiveM}" />
            <Button HorizontalAlignment="Right" Margin="2" Style="{StaticResource AccentedSquareButtonStyle}" Width="125" Content="Login Using Steam"  Command="{Binding LoginViaSteam}"/>
        </StackPanel>

    </StackPanel>
</UserControl>
